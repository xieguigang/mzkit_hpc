imports "MsImaging" from "hpc";

require(mzkit);

const .onLoad = function() {
    print(["Build high performance computing mzkit algorithm library based on Darwinism HPC runtime", 
           "An engineering practice of processing ultra massive lc-ms mass spectrometry rawdata, ms-imaging rawdata and single-cells metabolomics rawdata."
    ]);
}
